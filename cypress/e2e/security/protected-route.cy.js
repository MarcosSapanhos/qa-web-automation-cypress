describe('Segurança - Rota protegida', () => {

  it.skip('Não deve permitir acesso ao dashboard sem estar logado', () => {
    // O OrangeHRM demo não bloqueia corretamente via UI
    // Cenário mantido apenas como documentação
  });

});
